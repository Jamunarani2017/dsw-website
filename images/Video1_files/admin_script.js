		jQuery(document).ready(function($) {			//Color Picker Initialization			$('.wp-color-picker-field').wpColorPicker();			//navigation			$('.group').hide();			var pdActivetab = '';			if (typeof(localStorage) != 'undefined' ) {				pdActivetab = localStorage.getItem("pdActivetab");			}			if (pdActivetab != '' && $(pdActivetab).length ) {				$(pdActivetab).fadeIn();			} else {				$('.group:first').fadeIn();			}			$('.group .collapsed').each(function(){				$(this).find('input:checked').parent().parent().parent().nextAll().each(				function(){					if ($(this).hasClass('last')) {						$(this).removeClass('hidden');						return false;					}					$(this).filter('.hidden').removeClass('hidden');				});			});			if (pdActivetab != '' && $(pdActivetab + '-tab').length ) {				$(pdActivetab + '-tab').addClass('nav-tab-active');			}			else {				$('.nav-tab-wrapper a:first').addClass('nav-tab-active');			}			$('.nav-tab-wrapper a').click(function(evt) {				$('.nav-tab-wrapper a').removeClass('nav-tab-active');				$(this).addClass('nav-tab-active').blur();				var clicked_group = $(this).attr('href');				if (typeof(localStorage) != 'undefined' ) {					var pdStr = $(this).attr('href');					var Valid = pdStr.match("wppc");					if(Valid){						localStorage.setItem("pdActivetab", pdStr);					}				}				$('.group').hide();				$(clicked_group).fadeIn();				evt.preventDefault();			});			//Image upload			var file_frame;			jQuery.fn.uploadMediaFile = function( button, preview_media ) {				var button_id = button.attr('id');				var field_id = button_id.replace( '_button', '' );				var preview_id = button_id.replace( '_button', '_preview' );				// If the media frame already exists, reopen it.				if ( file_frame ) {				  file_frame.open();				  return;				}				// Create the media frame.				file_frame = wp.media.frames.file_frame = wp.media({				  title: jQuery( this ).data( 'uploader_title' ),				  button: {					text: jQuery( this ).data( 'uploader_button_text' ),				  },				  multiple: false				});				// When an image is selected, run a callback.				file_frame.on( 'select', function() {				  attachment = file_frame.state().get('selection').first().toJSON();				  jQuery("#"+field_id).val(attachment.id);				  if( preview_media ) {					jQuery("#"+preview_id).attr('src',attachment.sizes.thumbnail.url);				  }				});				// Finally, open the modal				file_frame.open();			}			jQuery('.image_upload_button').click(function() {				jQuery.fn.uploadMediaFile( jQuery(this), true );			});			jQuery('.image_delete_button').click(function() {				jQuery(this).closest('td').find( '.image_data_field' ).val( '' );				jQuery( '.image_preview' ).remove();				return false;			});									var wppcob = {				hide:function(id){					$('#'+id).parent().parent().hide();				},				show:function(id){					$('#'+id).parent().parent().show();				},				def:function(){					if($('#pd_custom_button').prop("checked") == true){						this.show('pd_custom_button_class');					}else{						this.hide('pd_custom_button_class');					}				},				change:function(){					$(document).on('change','#pd_custom_button',function(){						wppcob.def();					});				},				init:function(){					this.def();					this.change(this);				}			};			wppcob.init();			        });